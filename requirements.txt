maine-forms-engine @ git+https://github.com/bedardandy/maine-forms-engine@v0.6.0
# AGPL-3.0-or-commercial (Artifex) — see the licensing notice in README.md
pymupdf>=1.24
pikepdf>=8.0       # shared-field runtime split (via maine-forms-engine)
PyYAML>=6.0        # form.yaml metadata (tools/gen_catalog.py, agent workflows)
openai>=1.0
# Optional: the MCP server (tools/agent_server.py) needs `pip install mcp`.
